/**
 * The Main (Frontend) package is used to visually display the game. It does not pass any arguments to the engine package other than
 * the JSONArray storing all the level information.
 */
package main;