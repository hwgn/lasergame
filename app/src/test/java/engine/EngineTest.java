package engine;

import main.*;
import org.junit.jupiter.api.*;
import static org.junit.jupiter.api.Assertions.*;

import java.util.*;

class EngineTest {
    @Test void testAppToEngine() {
        //App app = new App();
        //app.setup();
        //Engine engine = new LaserEngine(0, app);
        //assertNotNull(engine);

        //engine.getTiles();
        //assertEquals(engine.getTiles(), Level.initialize(app)[0].getTiles());
    }
}
